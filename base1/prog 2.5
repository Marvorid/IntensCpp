#include <iostream>
using namespace std;
int main()
{
    float a, V, S;
    cin >> a;
    V = a*a*a;
    cout << "Объем куба = " << V << endl;
    S = 6*a*a;
    cout << "Площадь поверхности = " << S << endl;
    return 0;
}
