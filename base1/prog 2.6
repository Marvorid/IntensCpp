#include <iostream>
using namespace std;
int main()
{
    float a, b, c, V, S;
    cin >> a >> b >> c;
    V = a*b*c;
    cout << "Объем  = " << V << endl;
    S = 2*(a*b + b*c + a*c);
    cout << "Площадь поверхности = " << S << endl;
    return 0;
}
