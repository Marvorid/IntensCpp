#include <iostream>
using namespace std;
int main()
{
    float a, b, c, V, S;
    cin >> a >> b >> c;     
    V = a * b * c;
    S = 2*с*(a + b);
    cout << "Объем = " << V << endl;
    cout << "Площадь = " << S << endl;
    return 0;
}
